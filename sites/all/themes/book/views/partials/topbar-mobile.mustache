<nav class="navbar navbar-default navbar-fixed-top visible-xs" role="navigation" id="">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle pull-right" data-toggle="collapse" data-target=".navbar-ex1-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <img src="{{path}}/static/images/header/logo.png" class="logo-mobile" alt="landportal.info" />
  </div>

  <div class="collapse navbar-collapse mobile-top navbar-ex1-collapse">

    <a href="/user/login" title="{{#labels}}{{signin}}{{/labels}}" class="">
      <i class="fa fa-user fa-lg" title="{{#labels}}{{signin}}{{/labels}}"></i>
      {{#application}}
        {{#user}}
          {{name}}
        {{/user}}
        {{^user}}
          {{#labels}}{{signin}}{{/labels}}
        {{/user}}
      {{/application}}
    </a>

    <div class="selected-language no-margin">
      {{#application}}
        {{#languages}}
          {{#selected}}
            <span class="language">{{code}}</span>
          {{/selected}}
          {{^selected}}
            <a href="?language={{code}}" title="{{language}}" class="language">{{code}}</a>
          {{/selected}}
        {{/languages}}
      {{/application}}
      <form id="language-selection" method="POST">
        <input type="hidden" name="language" id="selected-language" />
      </form>

    </div>

    <a href="/book">
      <button class="btn btn-default">
        land<strong>book</strong>
      </button>
    </a>

    <a href="/debate">
      <button class="btn btn-default">
        land<strong>debate</strong>
      </button>
    </a>

    <a href="http://legacy.landportal.info/resource-type/document" title="View on the legacy website" target="_blank">
      <button class="btn btn-default">
          land<strong class="landlibrary">library</strong>
      </button>
    </a>

    <button class="btn btn-default" disabled="disabled">
      land<strong>library</strong><br />({{#labels}}{{coming_soon}}{{/labels}})
    </button>

    <a href="/sitemap">{{#labels}}{{sitemap}}{{/labels}}</a>

  </div>
</nav>
